function ErrorMessage() {
  return (
    <div
      style={{
        color: "red",
        textAlign: "center"
      }}
    >
      Failed to load data.
    </div>
  );
}

export default ErrorMessage;